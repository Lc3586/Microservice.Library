{
  "Datas": {
    "Controllers": {
      "Sample": {
        "Server": "http://localhost:5000/Sample/",
        "EnableDatabaseCmd": false,
        "Actions": {
          "S201": {
            //地址
            "Path": "detail/{id}",

            //请求方式
            "Method": "POST",

            //参数
            "Paramters": { "id": 0 },

            //测试数据量（执行sql语句的次数）
            "TestDataCount": 25,

            //测试前要执行的sql语句（例如生成测试数据）（Id值使用占位符: {0} => {i:000}）
            "SetupTestDataCmd": [],

            //测试后要执行的sql语句（例如清理测试数据）（Id值使用占位符: {0} => {i:000}）
            "ClearTestDataCmd": []
          }
        }
      }
    }
  }
}