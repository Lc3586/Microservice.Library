using Library.DataMapping;
using System;

namespace Library.DataMapping.Application
{
    public class AutoMapperGenOptions
    {
        public AutoMapperGeneratorOptions AutoMapperGeneratorOptions { get; set; } = new AutoMapperGeneratorOptions();
    }
}
